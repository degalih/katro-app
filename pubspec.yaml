name: katro_v2
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.3.1
  http: ^0.13.4
  provider: ^6.0.2
  auto_animated: ^3.0.1
  flutter_expanded_tile: ^0.3.6
  badges: ^2.0.2
  flutter_native_splash: ^2.1.2

flutter_native_splash:
  color: "#ffffff"
  image: assets/images/logo-katro.png

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
